//!  70622.1102         VARIABLES

//!  70622.1102         FUNCTIONS

window.onload = function () {
  init();
};

const init = function () {
    ma
};

const main = function () {
  init();
  summation(10);
  c(summation(10));
};

const summation = function (n) {
  let sum = 0;

  for (let i = 1; i <= n; i++) {
    sum += i;
  }
  return sum;
};
